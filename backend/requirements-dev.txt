# The runtime image (docker/backend.Dockerfile) installs requirements.txt,
# not this file. This file is installed on top of it for the Dockerfile's
# `test` stage (used only by docker-full-suite in CI) and for local dev.
# Config for ruff/mypy/bandit lives in backend/pyproject.toml.
ruff==0.16.0
mypy==2.3.0
bandit==1.9.4

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-mock==3.15.1
