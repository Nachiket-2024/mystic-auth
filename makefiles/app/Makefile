# Your own Makefile targets. Ships empty - add whatever you want here
# (e.g. `deploy-staging:`, `lint:`). Included by the root Makefile after
# makefiles/mystic_auth/Makefile, so a target here with the same name overrides
# the upstream one. Upstream never edits this file again. See
# docs/mystic_auth/template-usage/ownership-split.md.

.PHONY: hello
hello:
	@echo hello-from-app
