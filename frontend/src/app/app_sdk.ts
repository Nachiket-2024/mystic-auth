/**
 * App-specific extension surface (see docs/mystic_auth/template-usage/overview.md).
 *
 * This is the counterpart to sdk.ts: sdk.ts re-exports the template's own
 * building blocks and is not meant to be hand-edited, this file is where a
 * project built on this template adds its own re-exports for its own domain
 * code, kept separate so template updates never conflict with app-specific
 * additions here.
 *
 * Empty by default, and deliberately kept that way upstream — every release
 * of this template ships this file empty, on purpose, so it can never
 * conflict on a `scripts/sync-upstream.sh` sync. That also means the
 * reverse: if a sync ever does show a change to this file coming from
 * upstream, keep YOUR version, not upstream's — upstream's is only ever the
 * empty starting point, and your customizations are the ones worth keeping.
 *
 * Add your own imports/exports below as your app grows.
 */
