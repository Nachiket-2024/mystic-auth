/**
 * App-specific extension surface (see docs/mystic_auth/template-usage/overview.md).
 *
 * Counterpart to sdk.ts: sdk.ts re-exports the template's own building
 * blocks and isn't meant to be hand-edited. This file is where a project
 * built on the template adds its own re-exports, so template updates never
 * conflict with app-specific additions.
 *
 * Ships empty on purpose in every template release, so it never conflicts
 * on a `scripts/mystic_auth/upstream-sync/sync-upstream.sh` sync. If a sync ever shows an upstream
 * change here, keep YOUR version: upstream's is only ever the empty
 * starting point.
 *
 * Add your own imports/exports below as your app grows.
 */
