# ---------------------------- App Config ----------------------------
# URL where the backend is reachable
BACKEND_BASE_URL=http://localhost:8000

# URL where the frontend is reachable. OAuth, verification, and password-reset
# links use this canonical origin. Put extra CORS origins below.
FRONTEND_BASE_URL=http://localhost:5173

# Optional, comma-separated extra CORS origins (e.g. staging alongside prod).
# Leave empty for the common single-origin case.
# FRONTEND_ADDITIONAL_BASE_URLS=https://staging.example.com,https://www.example.com

# ---------------------------- Frontend Config ----------------------------
# Used under `docker compose up` in dev and prod. Only fill in
# frontend/.env.example when running the frontend with `npm run dev`.
# Dev uses the backend directly, so keep this absolute for docker-compose.yml.
# For local docker-compose.local-prod.yml tests, set it empty to use nginx's
# same-origin API proxy. Set it back before using the dev-up helper again.
# Deployments without bundled nginx need the public backend origin here.
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_NAME=MysticAuth
# Leave blank. Bugsink fills this after startup. See SENTRY_DSN below.
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=

# ---------------------------- Database Config ----------------------------
# Prefilled so `docker compose up` works out of the box. Change the
# password (keeping DATABASE_URL in sync) before real use.
# Used when running via Docker
DATABASE_URL=postgresql+asyncpg://postgres:change_me_in_production@postgres:5432/mystic_auth

# Used when running the backend locally instead of in Docker
# DATABASE_URL=postgresql+asyncpg://postgres:change_me_in_production@localhost:5433/mystic_auth

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=mystic_auth

# ---------------------------- JWT Config ----------------------------
# Fake placeholder so the app boots out of the box. Generate your own
# (`openssl rand -base64 32`) before real use. Must be at least 32 characters.
SECRET_KEY=change_me_in_production_generate_your_own_random_32plus_char_key

ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_MINUTES=43200
JWT_ALGORITHM=HS256
RESET_TOKEN_EXPIRE_MINUTES=60

# ---------------------------- OAuth2 / Gmail Config ----------------------------
GOOGLE_CLIENT_ID=<your_google_client_id>
GOOGLE_CLIENT_SECRET=<your_google_client_secret>
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/oauth2/callback/google

# ---------------------------- Redis Config ----------------------------
# Leave blank for local dev. Set a strong password for any deployment
# reachable outside your machine, and mirror it into REDIS_URL below.
REDIS_PASSWORD=

# Used when running via Docker. If REDIS_PASSWORD is set, include it too:
#   REDIS_URL=redis://:<REDIS_PASSWORD>@redis:6379/0
REDIS_URL=redis://redis:6379/0

# Used when running the backend locally (Redis still runs in Docker)
# REDIS_URL=redis://localhost:6380/0

CACHE_DEFAULT_TTL=300

# ---------------------------- Email / SMTP Config ----------------------------
# Address emails are sent from (also the SMTP login username)
FROM_EMAIL=<your_google_email>

# Gmail App Password for the account above
GMAIL_APP_PASSWORD=<your_gmail_app_password>

# Reply-To address shown in email footers. Optional, defaults to FROM_EMAIL.
SUPPORT_EMAIL=<your_support_email>

# Optional, only needed if not using Gmail's SMTP
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# ---------------------------- App Name Config ----------------------------
# Product name shown in emails and API responses
APP_NAME=MysticAuth

# ---------------------------- Rate Limiter Config ----------------------------
# How long (seconds) an account is locked out after too many failed logins
LOGIN_LOCKOUT_TIME=300

# Failed login attempts allowed before an account is locked out
MAX_FAILED_LOGIN_ATTEMPTS=5

# How long (seconds) an IP is locked out after too many failed logins across any accounts
LOGIN_LOCKOUT_TIME_PER_IP=300

# Failed login attempts allowed from one IP (across any accounts) before it's locked out.
# Set higher than MAX_FAILED_LOGIN_ATTEMPTS since one IP can be many real users (e.g. office wifi).
MAX_FAILED_LOGIN_ATTEMPTS_PER_IP=20

# General API rate limit. Max requests per window, then window duration.
MAX_REQUESTS_PER_WINDOW=100
REQUEST_WINDOW_SECONDS=60

# ---------------------------- Logging Config ----------------------------
# DEBUG, INFO, WARNING, or ERROR. Optional, defaults to INFO.
LOG_LEVEL=INFO

# ---------------------------- Environment Config ----------------------------
# "development" or "production". Optional, defaults to development.
# Production hides /docs, /redoc, and /openapi.json.
ENVIRONMENT=development

# ---------------------------- Reverse Proxy Config ----------------------------
# Comma-separated IPs of trusted reverse proxies (e.g. nginx). Leave empty
# unless a real proxy sits in front of the backend.
# The production-style Compose files pin frontend/nginx to 172.28.0.10 and
# use it as the API proxy. Uncomment for that setup. For any other proxy, use
# that proxy's address. Leave empty when the backend is reached directly.
# TRUSTED_PROXY_IPS=172.28.0.10

# ---------------------------- Cloudflare Tunnel Config (docker-compose.local-prod.yml) ----------------------------
# Required for a stable URL that survives laptop restarts (no random
# *.trycloudflare.com URL, no port forwarding, works behind NAT). Free.
#
# 1. Create a free Cloudflare account, then in Zero Trust dashboard:
#    Networks -> Tunnels -> Create a tunnel -> Cloudflared -> name it.
# 2. Copy the token shown on the install step (a long string) into
#    TUNNEL_TOKEN below. You do not need to run the install command it
#    shows you; docker-compose.local-prod.yml's cloudflared service uses
#    this token directly.
# 3. Still in that tunnel's config: Public Hostname -> Add a public hostname.
#    - Subdomain/domain: the domain you're routing (see is-a.dev note below).
#    - Service: HTTP, http://frontend:80
# 4. Point FRONTEND_BASE_URL / BACKEND_BASE_URL / VITE_API_BASE_URL below at
#    that public hostname once it's live.
#
# No domain of your own? github.com/is-a-dev/register gives free subdomains
# (yourname.is-a.dev) via a PR adding a CNAME record to your tunnel's
# <tunnel-id>.cfargotunnel.com address (shown on the tunnel's overview page).
# Use that CNAME as the public hostname in step 3 above.
TUNNEL_TOKEN=

# ---------------------------- VPS Deployment Config (docker-compose.prod.yml) ----------------------------
# Only used by the Caddy service in docker-compose.prod.yml. Not read by the
# app itself, and irrelevant for docker-compose.yml / docker-compose.local-prod.yml.

# Required. Caddy requests a Let's Encrypt certificate for this domain. DNS
# must already point at this host's public IP before first start.
PUBLIC_DOMAIN=

# Optional but recommended. Let's Encrypt sends renewal/expiry notices here.
ACME_EMAIL=

# Optional. Set only if Bugsink's UI and browser-side VITE_SENTRY_DSN reports
# should be reachable publicly. See the commented block in docker/Caddyfile.
BUGSINK_PUBLIC_DOMAIN=

# ---------------------------- Error Monitoring Config ----------------------------
# Leave blank to disable. Auto-filled once Bugsink starts under
# `docker compose up`. See docs/mystic_auth/error-monitoring/overview.md.
SENTRY_DSN=

# Optional tag shown on every reported error (e.g. "production").
# Falls back to ENVIRONMENT above if unset.
SENTRY_ENVIRONMENT=

# BUGSINK_* configures the self-hosted Bugsink service, on by default with
# `docker compose up`. See docs/mystic_auth/error-monitoring/overview.md.

# Bugsink's own secret key (unrelated to this app's SECRET_KEY above).
# Prefilled so it boots out of the box. Generate your own
# (`openssl rand -base64 50`) before real use.
BUGSINK_SECRET_KEY=change_me_in_production_generate_your_own_random_50plus_char_key

# Admin login for Bugsink's own UI (local only, never emailed), prefilled
# so it boots out of the box. Change it if you want.
BUGSINK_SUPERUSER_EMAIL=admin@example.com
BUGSINK_SUPERUSER_PASSWORD=change_me_in_production

# URL where you'll open Bugsink's UI
BUGSINK_BASE_URL=http://localhost:8010
